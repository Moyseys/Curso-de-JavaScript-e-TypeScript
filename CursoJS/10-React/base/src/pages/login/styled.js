import styled from 'styled-components'

import {secondColor } from '../../config/colors'

export const Title = styled.h1`
    text-align: center;
    background-color: green;
`

export const Paragrafo = styled.p`
    font-size: 80px;
    background-color: ${secondColor};
`