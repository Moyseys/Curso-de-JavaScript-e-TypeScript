//             -5        -4       -3        -2        -1    
//              0         1        2         3         4    
let nome = ['Eduardo', 'Maria', 'Joana', 'Gabriel', 'Julia']

//.splice(índice, romove, adiciona)

//POP => const removidos  = nome.splice(-1, 1)

//SHIFT => const removidos  = nome.splice(0, 1)

//PUSH => nome.splice(nome.length, 0, 'Luiz')

//UNSHIFTnome.splice(0, 0, 'Luiz')

//const removidos  = nome.splice(1, Number.MAX_VALUE)

console.log(nome, removidos);